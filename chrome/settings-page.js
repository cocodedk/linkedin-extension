/**
 * Settings page main entry point
 */

import { initializeSettingsPage } from './settings-page-init.js';

// Initialize the settings page when DOM is loaded
document.addEventListener('DOMContentLoaded', () => {
  initializeSettingsPage();
});